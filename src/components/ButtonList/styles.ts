import styled from "styled-components";

export const ButtonWrapper = styled.div`
    position: absolute;
    right: 20px;
    bottom: 33px;
    z-index: 401;
    gap: 5px;
    display: flex;
    flex-direction: column;
`