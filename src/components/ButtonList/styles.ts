import styled from "styled-components";

export const ButtonWrapper = styled.div`
    position: absolute;
    right: 24px;
    bottom: 24px;
    z-index: 401;
    gap: 8px;
    display: flex;
    flex-direction: column;
`